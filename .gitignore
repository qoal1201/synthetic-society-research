# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Secrets / env
.env
.env.*
*.key

# Raw / un-anonymized data — never commit by default.
# Anonymized, aggregated extracts go in explicitly, not under raw/.
data/raw/
*.sqlite
*.db

# OS / editor
.DS_Store
node_modules/

# Quarto
/_site/
/.quarto/

**/*.quarto_ipynb

# Local-only — 개인 작업 메모 (공개 안 함)
CLAUDE.md

# Local-only — 세션 인계용 백로그
BACKLOG.local.md
