# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Secrets / env
.env
.env.*
*.key

# Raw / un-anonymized data — never commit by default.
# Anonymized, aggregated extracts go in explicitly, not under raw/.
data/raw/
*.sqlite
*.db

# OS / editor
.DS_Store
node_modules/
.obsidian/

# Quarto
/_site/
/.quarto/

**/*.quarto_ipynb

# Local-only — 개인 작업 메모 (공개 안 함)
CLAUDE.md
.claude/

# Local-only — 세션 인계용 백로그 + 3-repo 공통 수신함
BACKLOG.local.md
INBOX.md
_preview/

# Local-only — 논문 PDF 원본 (저작권/용량 — 공개 repo에 안 올림)
_papers/

# Local-only — 재현용 외부 repo 클론 + venv (스모크 테스트, 2026-07-06)
_repro/
_workspace/
